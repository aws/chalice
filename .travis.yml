language: python
sudo: false
env: HYPOTHESIS_PROFILE=ci
matrix:
  - TOXENV=py27
  - TOXENV=py36
install:
  - pip install tox
script:
  - tox
after_success:
  - if [[ $TEST_TYPE == 'coverage' ]]; tox -e coverage; fi
